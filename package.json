{
  "name": "stream-zip",
  "version": "1.0.2",
  "description": "zip together multiple streams into a single stream",
  "main": "index.js",
  "scripts": {
    "test": "node test",
    "codecov": "node test -c lcovonly && ./node_modules/.bin/codecov",
    "git-reset": "git reset --hard HEAD && git clean -df && git pull"
  },
  "author": "Anton Mokrushin <anton@mokr.org> (mokr.org)",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "am-node-tape-runner": "^1.1.1",
    "async": "^2.1.4",
    "codecov": "^1.0.1",
    "lodash": "^4.17.2",
    "tape": "^4.6.3"
  },
  "engines": {
    "node": ">=6.0.0"
  }
}
